export function OrangeGlow() {
  return (
    <div
      className="pointer-events-none absolute -top-60 -right-60 w-175 h-175 rounded-full"
      style={{
        background: 'radial-gradient(circle, rgba(255,85,0,0.28) 0%, transparent 70%)',
      }}
    />
  )
}
